# msp430lib



针对 **msp430f5529** 的一个简单的实用库。

主要包含以下模块：

- IIC 

  基于 GPIO (SCL P6.0, SDA P6.5) 实现 IIC 通信（仅支持写，因为它是为 OLED 提供支持的）。

- OLED

  OLED 显示使用库（默认分辨率128 x 64）。

目前支持开发环境：**CCS** (Code Composer Studio)



> 特别声明：该库代码源自学校课程实验示例代码，个人仅仅是对代码进行重组，使其结构更加合理，另外添加一些实用函数。